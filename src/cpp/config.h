/* This file is not automatically generated in GNU SETL, but the first
   file it #includes is.  */

#include "../config.h"

#define SUCCESS_EXIT_CODE 0
#define FATAL_EXIT_CODE   33

#include <limits.h>

#define BITS_PER_UNIT  CHAR_BIT
#define BITS_PER_WORD  (CHAR_BIT * sizeof (int))

#define HOST_BITS_PER_INT   (CHAR_BIT * sizeof (int))
#define HOST_BITS_PER_LONG  (CHAR_BIT * sizeof (long))
#define HOST_BITS_PER_WIDE_INT  HOST_BITS_PER_LONG
#define HOST_WIDE_INT           long

#define TARGET_BELL    '\a'
#define TARGET_BS      '\b'
#define TARGET_TAB     '\t'
#define TARGET_NEWLINE '\n'
#define TARGET_VT      '\v'
#define TARGET_FF      '\f'
#define TARGET_CR      '\r'
